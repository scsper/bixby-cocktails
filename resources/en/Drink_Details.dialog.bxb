dialog (Result) {
  match: Drink (drink)

  if (size(drink) > 1) {
    template ("I found #{size(drink)} different drinks with the name #{value(drink.name)}")
  } else {
    template ("Here's the ingredients for an #{value(drink.name)}")
  }
}
